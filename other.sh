a || b ; c && d || (a && c ; d )

( a > out || b ) && c > out2

c && ( a || b )

( a || b ) && ( c && d )

 a && ( b || c )

( a || b ) <in

( a || b )>out

( a || b )<in>out

c && d || ( a && ( c ; d ) | e > out ) > out3 | f < in2

a || b && c | d && e || f

#( (a||(b)|( (c)|f))||c&&( ( (a)||b)))

